x = {props.name}
y = {props.class}
averageAge = 19

function Display(props) {
return <h1>Display {props.name} from  {props.class} where average age of the student is {averageAge} </h1>
}

export default Display





x = {props.name}
y = {props.class}
averageAge = 19

function Display(props) {
return <h1>Display {props.name} from  {props.class} where average age of the student is {averageAge} </h1>

<div>Type the captcha: {1+2}</div>
function buttonPressed () {
  alert("Button is pressed")
}
}

export default Display


function Display(props) {
  return <h1>Display {props.name} from  {props.class} where average age of the student is {averageAge} </h1>
    <div>Type the captcha: {1+2}</div>
    function buttonPressed() {
      alert("Button is pressed")
      }
    <button onClick={buttonPressed}>Click me</button>
  }